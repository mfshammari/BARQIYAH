# برقية — مواصفة البناء الكاملة (SPEC)

> احفظ هذا الملف باسم `SPEC.md` في جذر المشروع، وابدأ جلسة Claude Code بـ: «اقرأ SPEC.md وابنِ المشروع كاملاً حسبه». كل القرارات هنا محسومة بعد مرحلة تصميم وبروتوتايب مكتملة — لا تعِد التفاوض عليها، ونفّذها كما هي.

---

## 0. الملخص التنفيذي

**برقية** منصة SaaS سعودية لإدارة دعوات الأعراس والمناسبات، قناتها الأساسية **واتساب**.

الرحلة: صاحب المناسبة ينشئ مناسبة ويشتري باقة ← يضيف المدعوين ← تُرسل الدعوة عبر واتساب ← المدعو يؤكّد ويحدد عدد الحاضرين بأزرار تفاعلية ← يصله باركود ← يُمسح عند الباب.

**الموديل:** دفع لكل مناسبة (لا اشتراك متكرر). الرصيد **بالمقاعد** لا بعدد الدعوات.

**بنية الحساب (مهم):** العميل له **حساب دائم** يجمع كل مناسباته عبر السنين — لا حساب لكل مناسبة. الحساب هو المركز، والمناسبة كيان بداخله. العميل نوعان يستخدمان النظام نفسه: **أفراد** (أب يزوّج أبناءه) و**قاعات ومنظّمو مناسبات** (عدة مناسبات متزامنة).

**اللغة:** كل الواجهات عربية RTL بالكامل. الأرقام تُعرض بالأرقام العربية الهندية (٠١٢٣٤٥٦٧٨٩) في واجهات العرض.

---

## 1. الستاك (محسوم)

- **Next.js** (App Router + TypeScript) — الواجهة و API routes.
- **Vercel** للاستضافة.
- **Supabase** — PostgreSQL + Auth + Storage (صور الدعوات).
- **Meta Cloud API** مباشرة (بلا BSP) — رقم منصة **واحد مشترك**.
- **Tailwind CSS** مع دعم RTL كامل، وطبقة design tokens مركزية.
- **بوابة الدفع:** Moyasar (مع طبقة مجرّدة تسمح باستبدالها بـTap).

استخدم Server Components و Server Actions حيثما أمكن. فعّل **RLS على كل جدول** من البداية — لا تترك جدولاً مكشوفاً.

---

## 2. الهوية البصرية (طبقتان — نفّذها حرفياً)

هوية واحدة بنبرتين: **الواجهة العامة رسمية ووقورة**، و**الداخل ألطف وأدفأ**. نفّذها كـCSS variables في مكان واحد.

### الخطوط

```
Aref Ruqaa      → الشعار «برقية» فقط
Amiri           → عناوين صفحة الهبوط والعناوين الاحتفالية
IBM Plex Sans Arabic → نصوص صفحة الهبوط
Tajawal         → كل نصوص وواجهات الداخل (لوحات العمل)
```

### الألوان

```css
/* مشترك */
--ivory:#FBFAF6;      /* خلفية عامة */
--paper:#FCFAF4;      /* خلفية أقسام متناوبة */
--ink:#282621; --muted:#6E695C; --line:#E6DFCE;
--red:#A3382A; --red-soft:#F6E4E0;

/* طبقة formal — صفحة الهبوط وكل ما يراه الزائر */
--green:#153A2B; --green-2:#1E4C39;
--gold:#B08A47; --gold-soft:#D8BE86; --gold-line:#E3D8BE;
/* زوايا: 2px (حواف رسمية) · مسافات أقسام: 104px */

/* طبقة soft — الداخل (صاحب المناسبة، الأدمن، الماسح) */
--green:#234B3A; --green-tint:#EDF3EF;
--gold:#C9A94E; --gold-tint:#F7F0DC; --gold-deep:#8A6D1F;
--gray-tint:#F1EFE9;
/* زوايا: 10–16px (ناعمة) · بطاقات بيضاء بحدود رفيعة */
```

### قواعد النبرة

- **الهبوط:** أخضر غامق بطل، ذهبي بارز، تباين عالٍ، مساحات واسعة، حواف حادة. إحساس بطاقة زواج مطبوعة.
- **الداخل:** نفس العائلة بدرجات فاتحة، زوايا مستديرة، شريط جانبي بخلفية `--green-tint`، بطاقات إحصاء ملوّنة هادئة (أخضر/ذهبي/رمادي). إحساس مساحة عمل مريحة.
- **الماسح:** الحد الأدنى — شاشة مسح كبيرة ونتيجة واضحة، موبايل أولاً.

### عناصر مميزة (signature)

- **بطاقة الدعوة**: خلفية خضراء متدرّجة `linear-gradient(160deg,#1A4433,#123528)`، إطاران ذهبيان متداخلان، مونوجرام «ب» في دائرة، وباركود مصغّر أسفلها.
- **المسطرة الذهبية**: خط رفيع متلاشٍ الطرفين بمعيّن `◆` في وسطه، تفصل عناوين الأقسام.

---

## 3. قاعدة البيانات (Supabase)

أنشئ الجداول التالية مع العلاقات والفهارس و RLS:

**`profiles`** — مرتبط بـ`auth.users`: `id`, `role` (enum: `admin_owner` | `admin_support` | `admin_reviewer` | `admin_finance` | `user` | `scanner`), `full_name`, `phone`, `is_active`, `created_at`.

> `user` حساب واحد لكل عميل؛ كونه مالكاً أو داعياً صفة تُشتق من علاقته بالمناسبة (`events.owner_id` أو صفّ في `inviters`)، لا من `role`.

**`packages`** — `id`, `name`, `seats`, `price`, `is_active`, `created_at`.

**`events`** — `id`, `owner_id`, `package_id`, `occasion_type`, `internal_name` (اسم داخلي يميّزها في قائمة العميل), `host_name` (الجهة الداعية في نص الدعوة), `celebrant_primary` (صاحب المناسبة الأول), `celebrant_secondary` (nullable — يُستخدم في الزواج فقط), `event_date_hijri`, `event_date_gregorian`, `event_weekday`, `event_time`, `venue`, `buyer_name`, `buyer_phone`, `template_id`, `image_url`, `seats_quota`, `status` (enum: `unpaid` | `active` | `closed`), `activated_at`, `activated_by` (nullable — فارغ = تفعيل تلقائي), `created_at`.

> **حقول أصحاب المناسبة تتغيّر بنوعها:** إذا كان النوع `wedding` تظهر خانتان (العريس في `celebrant_primary`، والعروس في `celebrant_secondary` وهي اختيارية). في بقية الأنواع تظهر خانة واحدة فقط بعنوان يتبدّل حسب النوع: الخاطب (خطوبة) · العريس (عقد قران) · الخرّيج (تخرّج) · المولود (مولود جديد) · الجهة (مناسبة رسمية).

> **التاريخ:** يُخزَّن هجرياً وميلادياً معاً مع اليوم والوقت، لأن مواعيد الأعراس تُحدَّد هجرياً وتُعرض للضيوف باليوم والتاريخ.

**`templates`** — `id`, `owner_id` (nullable؛ فارغ = قالب عام), `name`, `body_text`, `image_url`, `status` (enum: `draft` | `under_review` | `approved` | `rejected`), `rejection_reason`, `whatsapp_category` (enum: `utility` | `marketing`), `meta_template_name`, `meta_status`, `created_at`.

**`inviters`** — **الداعي ليس نوع حساب، بل صفة داخل مناسبة**. أي مستخدم في المنصة يمكن أن يكون مالكاً لمناسباته وداعياً في مناسبات غيره بنفس الحساب ونفس الرقم ونفس دفتر العناوين. هذا الجدول يربط مستخدماً بمناسبة بصفة داعٍ: `id`, `event_id`, `profile_id` (حساب دخول), `name` (كما يظهر في الدعوة، مثل «أم سوسو»), `phone`, `role_label` (المالك / داعٍ), `side_label` (أهل العريس / أهل العروس / …), `seats_quota` (حصته من مقاعد المناسبة), `template_id` (القالب الذي اختاره بنفسه من المكتبة), `invite_vars` (jsonb — قيم المتغيرات التي كتبها بحرية), `image_url` (صورة دعوته), `created_at`.

> **مجموع `seats_quota` للدعاة ≤ `events.seats_quota`.** المالك يوزّع الحصص، وما لم يُوزَّع يبقى في رصيده.

**`guests`** — `id`, `event_id`, `inviter_id`, `name`, `phone`, `max_seats`, `confirmed_seats` (nullable), `reminded_at` (nullable — وقت إرسال التذكير الوحيد), `status` (enum: `draft` | `sent` | `accepted` | `declined` | `expired` | `attended` | `failed`), `qr_token` (uuid unique, nullable), `scans_used` (default 0), `sent_at`, `responded_at`, `attended_at`, `failure_reason`, `created_at`.

**`scanners`** — `id`, `event_id`, `profile_id`, `label`, `created_at`.

**`transactions`** — `id`, `event_id`, `package_id`, `amount`, `method` (enum: `gateway` | `bank_transfer` | `manual`), `gateway_ref`, `status` (enum: `pending` | `paid` | `failed` | `refunded`), `paid_at`, `created_at`.

**`message_logs`** — `id`, `event_id`, `guest_id`, `direction`, `template_name`, `meta_message_id`, `status` (`sent`/`delivered`/`read`/`failed`), `error_code`, `cost`, `created_at`.

**`activity_logs`** — `id`, `actor_id` (nullable = النظام), `action`, `target_type`, `target_id`, `metadata` (jsonb), `created_at`.

**`contacts`** — دفتر العناوين الدائم للعميل: `id`, `owner_id`, `name`, `phone`, `group_label` (العائلة/الأصدقاء/العمل/…), `notes`, `created_at`. **فريد على (`owner_id`,`phone`)**.

**`contact_groups`** — `id`, `owner_id`, `name`, `created_at`.

> `guests` تُنشأ لكل مناسبة على حدة، ويُسمح بربطها اختيارياً بـ`contact_id` لتتبّع سجل الاستجابة عبر المناسبات. **لا يُستورد الدفتر تلقائياً** — العميل يختار منه يدوياً لكل مناسبة.

**`platform_settings`** — صف واحد: مفاتيح Meta، مزوّد الدفع، Webhook tokens.

**`marketing_optins`** — `id`, `phone`, `source` (مثل `post_event_cta`), `consented_at`, `event_id`. **هذا الجدول الوحيد المسموح استخدامه للتسويق.**

---

## 4. منطق الرصيد (جوهري — نفّذه بدقة)

الرصيد **بالمقاعد** بثلاث حالات:

- **مؤكّد** = مجموع `confirmed_seats` لمن حالته `accepted` أو `attended`.
- **محجوز** = مجموع `max_seats` لمن حالته `sent` (نحجز الحد الأقصى احتياطاً حتى يرد).
- **متاح** = `seats_quota − محجوز − مؤكّد`.

**القواعد:**

1. الدعوة تعرض حداً أقصى (مثال ٧)، والمدعو يؤكّد عدداً فعلياً ≤ الحد (مثال ٣). الفرق يعود للمتاح فوراً.
2. **الرسائل منفصلة عن المقاعد**: كل مدعو = رسالة مدفوعة واحدة مهما كان عدد مقاعده.
3. **منع الإرسال عند النقص**: إذا كان `max_seats` > المتاح، امنع الإرسال واعرض عدد المقاعد الناقصة.
4. الحجوزات المعلّقة تبقى محجوزة وتظهر بوضوح في اللوحة.
5. `max_scans = confirmed_seats` — الباركود يُمسح بعدد المقاعد المؤكّدة.

---

## 4.1 منطق التذكير (محسوم — نفّذه حرفياً)

التذكير رسالة واحدة تُرسل لمن لم يردّ، بشرطين صارمين:

1. **الاستحقاق:** مضى **٥ أيام كاملة** على `sent_at` والحالة ما زالت `sent` (لم يؤكّد ولم يعتذر).
2. **مرة واحدة أبداً:** يُشترط `reminded_at IS NULL`. بعد الإرسال يُضبط `reminded_at = now()`، ومن ضُبط له لا يدخل في أي دفعة تذكير لاحقة مهما تأخّر.

```sql
-- المؤهّلون للتذكير
WHERE status = 'sent'
  AND reminded_at IS NULL
  AND sent_at <= now() - interval '5 days'
```

**في الواجهة:** الزر يعرض عدد المؤهّلين فقط، ونافذة تأكيد تذكر العدد وتنصّ على «مرة واحدة فقط»، وبعد الإرسال يتحوّل الزر إلى حالة معطّلة «ذُكّروا ✓». والمدعو الذي ذُكّر تظهر عليه شارة **«ذُكّر»** في الجدول.

**نطاق التذكير:** المالك يذكّر مدعوّيه، وكل داعٍ يذكّر مدعوّيه هو فقط — الاستعلام مقيّد بـ`inviter_id`.

> التذكير رسالة واتساب مدفوعة، وتكراره على غير الراغبين يرفع شكاوى الحظر ويضرّ تقييم الرقم المشترك — لذلك القيد إلزامي لا خيار.

---

## 5. الدفع والتفعيل (محسوم)

**الدفع عبر البوابة ← تفعيل تلقائي فوري.** غير المدفوع ← موقوف بانتظار تفعيل يدوي من الفريق.

**حرج:** التفعيل يعتمد على **Webhook من بوابة الدفع** (`/api/payments/webhook`) لا على رجوع المستخدم للصفحة — لأن المستخدم قد يغلق المتصفح بعد الدفع. عند استلام Webhook ناجح: حدّث `transactions.status='paid'` و`events.status='active'`، واكتب في `activity_logs` بـ`actor_id=null` (النظام).

- تحقّق من توقيع الـWebhook.
- اجعل العملية **idempotent** (Meta والبوابات تعيد الإرسال).
- التفعيل اليدوي متاح لدور `admin_owner` و`admin_support` فقط، ويُسجَّل باسم المنفّذ.

---

## 6. تكامل واتساب

رقم واحد مشترك، اتصال مباشر بـMeta Cloud API.

**قالب واحد معتمد بمتغيّرات — لا قالب لكل داعٍ.** اعتماد Meta لكل قالب يستغرق ساعات إلى أيام، فلا يصلح أن ينشئ كل داعٍ قالباً خاصاً. البنية معتمدة مرة واحدة، والمحتوى يتغيّر داخلها:

```
تتشرّف {{1}} بدعوتكم لحضور {{2}} — {{3}}

{{1}} و{{2}}  ← يكتبهما الداعي بحرية
{{3}}         ← يُحقن آلياً من بيانات المناسبة (لا يكتبه أحد)

أم حمودي / زواج ابني حمودي / [الجمعة ٢٦ شوال، قصر ٣٣، ٠٩:٠٠ مساءً]
أم سوسو  / زواج ابنتي سوسو / [الجمعة ٢٦ شوال، قصر ٣٣، ٠٩:٠٠ مساءً]
```

**حرية الداعي — مستويان:** (١) يختار قالبه من المكتبة المعتمدة، واختيارات الدعاة مستقلة داخل المناسبة الواحدة. (٢) يكتب متغيّراته بحرية دون موافقة المالك ودون اعتماد جديد. وصورة الدعوة (header) متغيّر أيضاً، فلكل داعٍ صورته.

> **التاريخ والمكان والوقت ليست متغيّرات يحرّرها الداعي.** هي حقول المناسبة (`event_date_hijri`, `event_date_gregorian`, `event_weekday`, `event_time`, `venue`) تُحقن آلياً في دعوة كل داعٍ. مصدر تحريرها **واحد**: صاحب المناسبة من صفحة بيانات المناسبة، وأي تعديل ينعكس فوراً على دعوات جميع الدعاة — لأن اختلاف الموعد بين دعوتين في مناسبة واحدة خطأ فادح. تُعرض للداعي مقفلة مع بيان مصدرها.

**تحقق إلزامي قبل الحفظ** (قيود Meta على المتغيّرات): لا روابط، لا أسطر جديدة، لا رموز غير مدعومة، وحدّ أقصى للطول — مع رسالة خطأ واضحة وتعطيل الحفظ حتى يصحّح، لأن الرفض المتكرر يضرّ تقييم الرقم المشترك. و**معاينة حيّة** تُظهر الرسالة كما تصل (صورة + نص + زرّان) وتتحدّث مع كل حرف.

**الإرسال:** عند «أرسل الآن» → القالب المعتمد + متغيّرات الداعي + صورته → حدّث الحالة إلى `sent` واحجز المقاعد من **حصة الداعي** → سجّل في `message_logs` مع `inviter_id`.

**Webhook (`/api/whatsapp/webhook`):** تحقّق GET بـverify_token؛ POST يلتقط ضغط الأزرار (تأكيد/اعتذار) ← يحدّث الحالة ← عند التأكيد يطلب عدد الحاضرين ← يولّد `qr_token` ويرسل الباركود داخل نافذة الخدمة.

**طبقة `WhatsAppProvider` بوضع Mock إلزامية:** عند غياب مفاتيح Meta، يعمل النظام بمحاكاة كاملة (إرسال وهمي، ردود قابلة للتشغيل يدوياً) حتى لا يتعطّل البناء والاختبار بانتظار اعتماد Meta. عند إضافة المفاتيح يتحوّل تلقائياً للوضع الحقيقي.

**حماية الرقم المشترك:** سجّل معدل الشكاوى لكل عميل، وأتِح للأدمن إيقاف إرسال عميل مؤقتاً. انخفاض جودة الرقم يوقف كل العملاء دفعةً واحدة — عامله كخطر تشغيلي أول.

---

## 7. سياسة البيانات (إلزامية — لا تتجاوزها)

أرقام المدعوين والدعاة **بيانات أمانة** جُمعت لغرض واحد: إيصال دعوة. لا تُستخدم للتسويق ولا تُصدَّر ولا تُشارك — التزاماً بنظام حماية البيانات الشخصية السعودي (PDPL) وشروط واتساب.

- الأدمن يرى بيانات المدعوين **للدعم التشغيلي فقط**، وكل اطلاع يُسجَّل في `activity_logs`.
- التسويق مسموح لـ: **عملاء المنصة المسجّلين** (وافقوا عند التسجيل)، ومن سجّل في **`marketing_optins`** طوعاً.
- شاشة «الرؤى» تعرض **إحصائيات مجمّعة بلا هوية** فقط.
- «الدخول كالعميل» متاح لـ`admin_owner`/`admin_support`، ويُسجَّل إلزامياً.
- **دفتر العناوين** يخزّن أرقاماً طويل المدى، فيجب: زر حذف لكل جهة، وزر حذف الدفتر كاملاً، وتصدير بيانات العميل عند طلبه (حق الوصول والمحو في PDPL). الدفتر خاص بمالكه ولا يظهر لأي عميل آخر ولا يُستخدم للتسويق.

---

## 8. الصفحات

### 8.1 العامة

- **`/`** — صفحة الهبوط بترتيب: هيدر ← هيرو (عنوان + بطاقة الدعوة) ← شريط ثقة أخضر ← كيف تعمل (٤ خطوات مرقّمة) ← المميزات (٦) ← أنواع الاحتفالات (٦) ← الباقات (٣، الوسطى مميّزة) ← الأسئلة الشائعة (٦) ← دعوة ختامية ← فوتر.
  > الترتيب مقصود: القيمة قبل السعر، والأسئلة بعده لإزالة التردد.
- **`/login`**, **`/signup`**
- **`/rsvp/[token]`** — صفحة RSVP عامة (نسخة ويب تكميلية لتدفّق واتساب).
- **`/i/[qr_token]`** — عرض الباركود للمدعو المؤكِّد.

### 8.2 صاحب المناسبة — **مستويان**

**المستوى الأول — الحساب (دائم):**

- **`/app`** «مناسباتي» — نقطة الدخول بعد تسجيل الدخول. بطاقات لكل مناسبة (نشطة/قادمة/مسودة/منتهية) مع: الحالة، الموعد، المكان، الباقة، شريط تقدّم المقاعد، وأرقام (مؤكّد/محجوز/متاح). المنتهية تعرض نتيجة الحضور ورابط التقرير. وتبويبات تصفية + ملخّص الحساب (عدد المناسبات، حجم الدفتر، متوسط التأكيد).
- **`/app/contacts`** — **دفتر العناوين**: جهات دائمة بمجموعات وبحث، استيراد وتصدير، وعمود **سجل الاستجابة** (يرد دائماً / يتأخر / نادراً) محسوب من مناسباته السابقة. أزرار حذف الجهة وحذف الدفتر.
- **`/app/billing`** — سجل المشتريات والفواتير.
- **`/app/settings`** — بيانات الحساب.
- **`/app/events/new`** — إنشاء مناسبة بأربع خطوات: التفاصيل ← الباقة ← القالب ← الدفع.
  - **التفاصيل** تشمل: نوع المناسبة، أصحاب المناسبة (حقلان للزواج / حقل واحد لغيره بعنوان متبدّل)، «الدعوة باسم» (الجهة الداعية)، التاريخ الهجري والميلادي واليوم والوقت، المكان، و«اسم المناسبة» الداخلي.
  - تُعرض **معاينة حيّة لنص الدعوة** أسفل النموذج تتحدّث مع الإدخال، ليرى العميل كيف ستصل دعوته قبل الشراء.

**المستوى الثاني — داخل المناسبة** (`/e/[id]/…`) — مع زر رجوع ظاهر «← كل مناسباتي»:

- **`/e/[id]`** — لوحة المعلومات. **قاعدة منع التكرار:** الأعلى «ما يحتاج انتباهك» (بطاقات إجراء فقط: مسوّدات لم تُرسل · لم يردّوا منذ ٥ أيام · فشل الإرسال) ثم الرصيد بثلاث حالات ثم أحدث المدعوين. **لا تُكرَّر عدّادات الحالات هنا** — مكانها شريط تصفية في صفحة المدعوين. ولا يُعرض رقمان لنفس المعنى في الشاشة. إذا لم يوجد معلّق يختفي القسم وتظهر حالة «كل شيء تحت السيطرة».
- **`/e/[id]/guests`** — المدعوون: **شريط تصفية بالحالات** أعلى الجدول (الكل · أكّد · بانتظار الرد · اعتذر · حضر · مسودة · فشل) بأعداده — وهو المكان الوحيد لعدّادات الحالات. ثم إضافة فردية، رفع Excel، و**اختيار من دفتر العناوين** (اختيار متعدد، غير إجباري)، حقل عدد الأشخاص، تبديل «أرسل الآن/مسودة»، و**إرسال جماعي للمسودات دفعةً واحدة**.
- **`/e/[id]/inviters`** — إدارة الدعاة: إنشاء حساب لكل داعٍ (اسم + جوال للدخول)، و**تخصيص حصته من المقاعد** بشريط توزيع يبيّن الموزَّع وغير الموزَّع. ويرى المالك أداء كل داعٍ (أُرسل/أكّد/متاح له).

> **ملكية النص للداعي وحده:** المالك **يعرض** نصّ الداعي وقالبه وصورته **للاطلاع فقط ولا يملك تعديلها** — لا عند الإنشاء ولا بعده. عند الإضافة يحدّد المالك الاسم والجوال والصفة والحصة فقط، ثم يصل الداعي رابط دخول يختار عنده قالبه ويكتب نصّه بنفسه؛ وقبل ذلك تظهر حالته «لم يكتب نصّه بعد». صلاحية المالك على الداعي محصورة في: تعديل الحصة، وإعادة إرسال رابط الدخول، وإلغاء الحساب.

- **`/e/[id]/template`** — اختيار قالب معتمد أو طلب قالب خاص.
- **`/e/[id]/scanners`** — حسابات المسح.
- **`/e/[id]/info`** — بيانات المناسبة وحالة الدفع والترقية.

### 8.3 الأدمن (مجمّعة في القائمة)

**التشغيل اليومي:**

- **`/admin`** «اليوم» — **لوحة مهام لا لوحة أرقام**: تنبيه جودة واتساب عند الخطر، بطاقات مهام (طلبات غير مدفوعة / قوالب قيد المراجعة / تذاكر دعم) بأعدادها وأقدميتها، مناسبات خلال ٧ أيام **بعمود الجاهزية**، ونبض اليوم (أُرسل/فشل/تأكيدات/مسح/إيراد).
- **`/admin/whatsapp`** — تقييم الجودة، طبقة الإرسال، أعلى العملاء في الشكاوى **مع زر إيقاف مؤقت**، أسباب الفشل، حالة القوالب في Meta بتصنيفها.
- **`/admin/events`** — قائمة بتبويبات (الكل/غير مدفوعة/نشطة/قريبة/منتهية) + بحث وتصفية فعّالة، مع **مبدّل عرض: جدول ⇄ تقويم**.
- **التقويم** (نفس المسار، وضع عرض) — شهر كامل، كل مناسبة في يومها ملوّنة بالجاهزية (جاهزة/تحتاج متابعة/غير مدفوعة)، مع تظليل **الأيام المزدحمة** وتنبيه يربط الازدحام بضغط إرسال واتساب. يدعم **التقويم الهجري إلى جانب الميلادي** (مواعيد الأعراس تُحدَّد هجرياً غالباً).
- **`/admin/events/[id]`** — **صفحة تفاصيل المناسبة**: مسار تنقّل، شرائح الحالة، تبويبات داخلية، الرصيد، **أداء الدعاة بمعدّل تأكيد كل داعٍ**، صحة الإرسال بالتكلفة، جدول المدعوين، «الدخول كالعميل»، تقرير المناسبة.
- **`/admin/templates/requests`** — مراجعة الطلبات مع تحديد تصنيف Meta، وقبول/رفض بسبب/طلب تعديل.
- **`/admin/support`** — تذاكر بأولوية مع إجراءات سريعة.

**الإدارة:** `/admin/clients` (مع الإنفاق وآخر نشاط)، `/admin/finance` (إيراد + تحصيل معلّق + سجل معاملات يبيّن طريقة الدفع وآلية التفعيل)، `/admin/insights` (الرؤى المجمّعة + فرص تسويقية مشروعة)، `/admin/packages`، `/admin/templates`.

**النظام:** `/admin/team` (أعضاء بأدوار + **مصفوفة صلاحيات**)، `/admin/activity` (سجل نشاط باسم المنفّذ، والنظام كمنفّذ للتفعيل التلقائي)، `/admin/settings` (Meta + بوابة الدفع + Webhooks).

**تجربة الأدمن:** بحث سريع عام (⌘K) يغطي العملاء والمناسبات والمدعوين، مسار تنقّل في صفحات التفاصيل، وأزرار الجداول تنقل فعلياً لصفحة التفاصيل.

### 8.4 صفة الداعي (داخل حساب المستخدم نفسه)

**لا يوجد دور «داعٍ» منفصل ولا حساب خاص به.** المستخدم الواحد يرى في «مناسباتي» قسمين:

1. **مناسباتي** — التي يملكها (اشترى باقتها).
2. **مناسبات أنا داعٍ فيها** — دعاه أصحابها، لكل واحدة حصته ونصّه.

فتح مناسبة يملكها → لوحة المناسبة الكاملة. فتح مناسبة هو داعٍ فيها → **مساحته وحده**:

- **حصته من المقاعد** بثلاث حالات (متاح لـه / محجوز / مؤكّد) — **محسوبة داخل حصته فقط**. لا يرى إجمالي المناسبة ولا مقاعد بقية الدعاة.
- **بطاقة المناسبة الثابتة** أعلى الشاشة: الاسم، صاحب المناسبة، عدّاد تنازلي، والتاريخ الهجري والميلادي والوقت والمكان — للقراءة فقط مع بيان أنها من صاحب المناسبة.
- **حصته** بشريط توزيع وثلاث بطاقات (متاح/محجوز/مؤكّد) محسوبة داخل حصته.
- **«ما يحتاج انتباهك»**: مسوّدات لم تُرسل · لم يردّوا منذ ٥ أيام (تذكير واحد فقط) · مقاعد متاحة — بأزرار إجراء مباشرة، وبلا تكرار للعدّادات.
- **تبويبان: دعوتي · مدعوّوي**. في «دعوتي» خطوتان مرقّمتان: اختيار القالب، ثم كتابة نصّه (الاسم وصلته بالمناسبة فقط) مع **صف مقفل يعرض الموعد والمكان**، ومعاينة حيّة كما تصل على واتساب.
- **مدعوّوه** بجدول فيه إجراء لكل صف حسب حالته (أرسل / ذكّره / الباركود)، مع الإضافة من دفتر عناوينه أو رفع Excel.
- **مدعوّوه فقط**: إضافة فردية، رفع Excel، إرسال ومتابعة. لا يرى مدعوّي غيره.
- **دفتر عناوينه الشخصي متاح له** هنا أيضاً — يختار منه مدعوّيه كما يفعل في مناسباته.
- عند نفاد حصته: رسالة تطلب منه مراجعة صاحب المناسبة (المالك وحده يوزّع الحصص).

> **الدعوة للانضمام:** حين يضيف المالك داعياً برقم جوال، إن كان الرقم لمستخدم قائم أُضيفت المناسبة إلى قسم «مناسبات أنا داعٍ فيها» عنده مباشرة؛ وإن كان جديداً وصله رابط ينشئ به حسابه — وهو الحساب نفسه الذي سيستخدمه لاحقاً لو أراد شراء باقة لمناسبته الخاصة.

> **عزل إلزامي عبر RLS:** استعلامات الداعي مقيّدة بـ`inviter_id` الخاص به. تسريب مقاعد أو مدعوّي طرف لطرف آخر خطأ جسيم — خصوصاً بين أهل العريس وأهل العروس.

### 8.5 الماسح

- **`/scan/login`** ثم **`/scan`** — شاشة مسح كبيرة، تحقّق أونلاين، نتيجة واضحة: نجاح («خالد الفهد · مسح ٢ من ٤») أو رفض («الكود مستخدم بالكامل» / «غير صالح»). موبايل أولاً.

---

## 9. متطلبات تجربة الأدمن (إلزامية)

1. **إجراءات جماعية**: خانات اختيار في كل الجداول الطويلة (المناسبات، الطلبات، المدعوين) مع شريط إجراء يظهر عند التحديد (تفعيل/رفض/تصدير جماعي).
2. **تأكيد قبل الإجراءات الخطيرة**: نافذة تأكيد تشرح الأثر قبل إيقاف إرسال عميل، رفض قالب، أو حذف — الإيقاف الخاطئ قبل حفل بيوم يعطّل العميل بالكامل.
3. **إخفاء ما لا يملكه الدور**: عناصر القائمة تُبنى من صلاحيات المستخدم لا ثابتة (المحاسب لا يرى «مراقبة واتساب»).
4. **حالات فارغة وحالات تحميل** لكل شاشة — الشاشة البيضاء توحي بعطل.
5. **إشعارات داخلية** للأحداث الحرجة (انخفاض جودة الرقم، طلب عاجل، فشل إرسال جماعي) لا تنتظر فتح الشاشة.
6. **بحث عام (⌘K)** يغطي العملاء والمناسبات والمدعوين.
7. **إجراء سريع «أرسل تذكيراً لمن لم يرد»** داخل صفحة المناسبة — أكثر طلب متكرر قبل الحفل.
8. القائمة تُجمَّع في ثلاث مجموعات: التشغيل اليومي · الإدارة · النظام. والشاشات نادرة الاستخدام (الباقات، مكتبة القوالب) تُدمج داخل الإعدادات كتبويبات.

---

## 10. مصفوفة الصلاحيات

| الصلاحية | مدير | دعم | مراجع | محاسب |
|---|---|---|---|---|
| تفعيل يدوي (غير المدفوع) | ✓ | ✓ | — | — |
| مراجعة القوالب | ✓ | — | ✓ | — |
| الدخول كالعميل | ✓ | ✓ | — | — |
| المالية والتقارير | ✓ | — | — | ✓ |
| إعدادات واتساب | ✓ | — | — | — |
| إدارة الفريق | ✓ | — | — | — |

---

## 11. متغيرات البيئة

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
META_PHONE_NUMBER_ID=
META_WABA_ID=
META_ACCESS_TOKEN=
META_WEBHOOK_VERIFY_TOKEN=
MOYASAR_SECRET_KEY=
MOYASAR_WEBHOOK_SECRET=
APP_URL=
```

غياب مفاتيح Meta أو الدفع يفعّل وضع Mock بدل الانهيار.

---

## 12. ترتيب البناء

ابنِ المشروع كاملاً، بهذا الترتيب الداخلي:

1. **الأساس** — Next.js + Supabase (جداول + RLS + Auth) + الأدوار + tokens الهوية بطبقتيها + تخطيط RTL.
2. **صفحة الهبوط** — بالهوية الرسمية والترتيب المحدد.
3. **صاحب المناسبة** — مستوى الحساب («مناسباتي» + دفتر العناوين) ثم إنشاء المناسبة والدفع، ثم لوحة المناسبة والمدعوين ومنطق الرصيد.
4. **واتساب والباركود** — WhatsAppProvider (Mock + Real) + الإرسال + Webhook + توليد QR.
5. **الأدمن** — الشاشات الأربع عشرة + الصلاحيات + سجل النشاط.
6. **المسح** + **RSVP العامة**.

أنشئ بيانات تجريبية (seed) لكل جدول لتُختبر كل شاشة فوراً.

---

## 13. معايير القبول

- عربي RTL نظيف ومتجاوب؛ موبايل أولاً للعميل والماسح.
- الهوية بطبقتيها مطبّقة: الهبوط رسمي، الداخل لطيف — من tokens مركزية.
- RLS تمنع أي تسريب بين المناسبات أو العملاء.
- منطق الرصيد الثلاثي دقيق، ومنع الإرسال عند النقص يعمل.
- الدفع عبر البوابة يفعّل المناسبة تلقائياً عبر Webhook (اختبرها بإغلاق المتصفح فور الدفع).
- الإرسال والـWebhook والباركود والمسح تعمل كاملة في وضع Mock.
- كل إجراء إداري مسجَّل في `activity_logs` باسم منفّذه.
- لا تصدير ولا استخدام تسويقي لأرقام المدعوين في أي مسار.
- العميل يسجّل دخوله فيصل إلى **«مناسباتي»** لا إلى مناسبة بعينها، ويتنقّل بين مناسباته بحرية.
- دفتر العناوين يعمل: إضافة، تجميع، بحث، اختيار منه عند إضافة المدعوين، وحذف.
- **عزل الدعاة يعمل**: داعٍ يسجّل دخوله فيرى حصته ومدعوّيه فقط، ولا يصل بأي استعلام إلى بيانات داعٍ آخر أو إجمالي المناسبة.
- كل داعٍ يرسل بنصّه وصورته الخاصة عبر القالب المعتمد نفسه.
- التقويم يعرض المناسبات في أيامها ويبرز الأيام المزدحمة.
- الإجراءات الجماعية والتأكيدات وإخفاء العناصر حسب الدور تعمل فعلياً.
- يبني وينشر على Vercel بلا أخطاء.

---

## 14. نقاط مفتوحة

عند مواجهتها، طبّق الخيار الأبسط الآمن وضع `// TODO: confirm with owner`:

- **سياسة الاسترداد** بعد التفعيل التلقائي — الموصى به: لا استرداد بعد أول إرسال (الرسائل كلّفت فعلياً)، أو استرداد جزئي بخصم المستهلك.
- تصنيف القوالب (Utility مقابل Marketing) وأثره السعري.
- هل متابعة الباركود رسالة حرة داخل نافذة الخدمة أم قالب مدفوع منفصل.
- الحجم الشهري المتوقع (يحدد ضغط طبقة الإرسال في الموسم).
